# Status Brief（自動產生）

> 這份檔案由 `command-center/scripts/collect-status.sh` 自動產生，**請勿手動編輯**。
> 要改格式請改腳本；要改掃描範圍請改 `command-center/repos.txt`。
>
> 目前尚未跑過。設定 `STATUS_TOKEN` secret 後，Collect Status workflow 會自動填入內容
> （或在本機執行 `bash command-center/scripts/collect-status.sh`）。
